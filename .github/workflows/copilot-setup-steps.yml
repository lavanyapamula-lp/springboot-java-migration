name: "Copilot Agent Environment Setup"

on:
  workflow_dispatch: # Allows you to manually verify the setup

jobs:
  # The agent specifically looks for this ID to prepare its workspace
  copilot-setup-steps:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: 'maven'

      - name: Verify Environment
        run: |
          java -version
          mvn -version